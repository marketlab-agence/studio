import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function RemoteConnectionVisual() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Visualisation des Connexions Distantes</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour la visualisation des connexions aux dépôts distants.</p>
      </CardContent>
    </Card>
  );
}
