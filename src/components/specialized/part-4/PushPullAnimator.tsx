import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function PushPullAnimator() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Animateur Push/Pull</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour l'animation des commandes push et pull.</p>
      </CardContent>
    </Card>
  );
}
