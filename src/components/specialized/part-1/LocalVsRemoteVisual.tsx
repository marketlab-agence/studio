import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function LocalVsRemoteVisual() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Visualisation Local vs Distant</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour la visualisation des concepts de dépôt local et distant.</p>
      </CardContent>
    </Card>
  );
}
