import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function GitVsGitHubComparison() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Comparaison Git vs GitHub</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour la comparaison visuelle entre Git et GitHub.</p>
      </CardContent>
    </Card>
  );
}
