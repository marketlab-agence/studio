import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function PRWorkflowSimulator() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Simulateur de Workflow de Pull Request</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour le simulateur de workflow de Pull Request complet.</p>
      </CardContent>
    </Card>
  );
}
