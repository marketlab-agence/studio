import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function CodeReviewInterface() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Interface de Revue de Code</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour l'interface simulée de revue de code.</p>
      </CardContent>
    </Card>
  );
}
