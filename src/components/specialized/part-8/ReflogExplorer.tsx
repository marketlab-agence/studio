import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ReflogExplorer() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Explorateur Reflog</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour l'explorateur interactif du reflog.</p>
      </CardContent>
    </Card>
  );
}
