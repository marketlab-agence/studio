import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function TimelineNavigator() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Navigateur de Timeline</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour le navigateur interactif de l'historique (timeline).</p>
      </CardContent>
    </Card>
  );
}
