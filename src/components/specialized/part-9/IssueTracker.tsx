import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function IssueTracker() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Traqueur d'Issues Simulé</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour le traqueur d'issues simulé.</p>
      </CardContent>
    </Card>
  );
}
