import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function RepoCreationWizard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Assistant de Création de Dépôt</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour l'assistant pas-à-pas de création de dépôt.</p>
      </CardContent>
    </Card>
  );
}
