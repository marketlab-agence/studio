import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function StagingAreaVisualizer() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Visualiseur de Zone de Staging</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour le visualiseur interactif de la zone de staging.</p>
      </CardContent>
    </Card>
  );
}
