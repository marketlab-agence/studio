import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function CommitHistoryExplorer() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Explorateur d'Historique de Commits</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour l'explorateur d'historique des commits.</p>
      </CardContent>
    </Card>
  );
}
