import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function WorkflowSimulator() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Simulateur de Workflow</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour le simulateur de workflows (ex: GitFlow).</p>
      </CardContent>
    </Card>
  );
}
