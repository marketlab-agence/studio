import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function WorkflowComparisonTable() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Tableau Comparatif des Workflows</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour le tableau comparatif des différents workflows Git.</p>
      </CardContent>
    </Card>
  );
}
