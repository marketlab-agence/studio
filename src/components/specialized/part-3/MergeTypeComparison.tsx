import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function MergeTypeComparison() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Comparaison des Types de Fusion</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour comparer visuellement 'merge', 'squash' et 'rebase'.</p>
      </CardContent>
    </Card>
  );
}
