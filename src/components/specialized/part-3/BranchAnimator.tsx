import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function BranchAnimator() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Animateur de Branches</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour l'animation de la création et fusion de branches.</p>
      </CardContent>
    </Card>
  );
}
