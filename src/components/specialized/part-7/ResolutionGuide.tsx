import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ResolutionGuide() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Guide de Résolution de Conflits</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Contenu pour le guide pas-à-pas de résolution de conflits.</p>
      </CardContent>
    </Card>
  );
}
