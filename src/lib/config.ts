// This is a simple way to define an admin user.
// For a production app, it is recommended to use a more robust role system like Firebase custom claims.
export const ADMIN_EMAIL = 'admin@katalyst.com';
