/**
 * Fichier d'utilitaires pour la coloration syntaxique.
 * Pour une implémentation réelle, on utiliserait une bibliothèque comme 'prism-react-renderer' ou 'react-syntax-highlighter'.
 */

export function highlightCode(code: string, language: string): string {
  // Ceci est un placeholder. Une vraie fonction transformerait le code en HTML coloré.
  console.log(`Highlighting code for language: ${language}`);
  return code;
}
